{
    "name": "first-grpc",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "proto-gen": "rm -rf src/protoDist/ && mkdir -p src/protoDist && proto-loader-gen-types --longs=Number --enums=String --defaults --oneofs --keep-case --grpcLib=@grpc/grpc-js -O ./src/protoDist ./proto/*.proto",
        "start:server": "tsc && node ./dist/server/index.js"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "@grpc/grpc-js": "^1.14.3",
        "@grpc/proto-loader": "^0.8.0",
        "@grpc/reflection": "^1.0.4"
    },
    "devDependencies": {
        "@types/node": "^25.0.3",
        "typescript": "^5.9.3"
    }
}
